# GIT :- Git is a version control system. Its Tool

It tracks: code changes, project history, who changed what, previous versions

Think of Git like: “Save points for your code project”

# GITHUB :- GitHub is a cloud platform where Git repositories are stored online.

GitHub helps with: backup, collaboration, sharing code, team projects, deployment integration

PHASE 1 - BASICS :-
git init
add
commit
status
log

PHASE 2 - GITHUB :-
create repo
push
pull
clone

PHASE 3 - BRANCHING :-
branch
checkout
merge
conflicts

PHASE 4 - ADVANCED :-
stash
reset
revert
SSH
PR workflow

# GitHub Workflow (MOST COMMON) :-
Clone Repo
↓
Create Branch
↓
Write Code
↓
Commit
↓
Push Branch
↓
Create Pull Request
↓
Review
↓
Merge


# Git File Lifecycle (VERY IMPORTANT): A file moves through these stages :-
Working Directory (our local repo)
↓
Staging Area (source control)
↓
Repository (main - remote/Github repo)


# Typical Daily Workflow :-
Write code
↓
Make several changes
↓
git add .
↓
git commit -m "Meaningful update"
↓
git push
That’s how developers usually work.


Important                Terms
Repository (Repo)	  Project folder tracked by Git
Commit	              Snapshot/save point
Branch	              Separate version of code
Merge	              Combine branches
Clone	              Copy repo from GitHub
Push	              Upload code to GitHub
Fetch                 Fetch downloads only.
Pull	              Downloads latest code + Merge
Remote	              Online GitHub repo
Local Repo	          Repo on your computer


# Important Git Commands Summary :-
Command	               Purpose
git init	            Start Git
git status	            Check changes
git add .	            Stage files
git commit -m ""	    Save snapshot
git log	                History
git branch	            View branches
git checkout	        Switch branch
git merge	            Merge branches
git push	            Upload
git pull	            Download + Merge
git clone	            Copy repo
git stash	            Temporary save


# STEPS TO USE GIT :- 

1. Git Setup :-
git config --global user.name 'your name(globally it will used everywhere)'
git config --global user.email 'your email Id '


# COMMANDS :-

2. git init :-
This creates hidden .git folder, Now Git starts tracking project.

3. git status :- 
Check Status

4. git add .  :-
Add all Files to Staging

5. git commit -m "Initial commit" :-
Commit message should explain changes.

6. git log / git log --oneline :-
Check Commit History

7. git branch feature-login :-
Create New Branch

git branch
All Local branches

git branch -r
All Remote branches

git branch -a
Both Local and Remote branches

8. git checkout feature-login / modern-way - git switch feature-login
Switch Branch

9. git checkout -b feature-login :-
Create + Switch Together

10. git branch :-
See All Branches, Current branch has *

11. git merge feature-login :-
Now feature code added to main.

12. .gitignore :-
Some files should NOT go to GitHub.
Examples: node_modules, .env, build folders 

PUSH LOCAL PROJECT TO GITHUB :-
S-1 : We have to create Repo on GitHub
S-2 : Connect Local Repo :-
13. git remote add origin https://github.com/VK18-HRSH/localrepo.git

PUSH CODE : For First Time :-
14. git push -u origin main
LOCAL → GITHUB
Upload local commits to remote GitHub repo
Git remembers : local main ↔ GitHub main
After That :- git push

15. git clone repo-url :-
Clone Repository, Download repo from GitHub

16. git pull origin main :-
GITHUB → LOCAL
Download + merge remote changes into local repo

17. git stash :-
Temporarily save unfinished work.

18. git stash pop :-
restore + remove stash

19. git stash apply :-
restore only

20. git stash list :-
See Stored Stashes

21. git reset --soft HEAD~1 :-
Soft Reset.

22. git reset HEAD~1 :-
Mixed Reset.

23. git reset --hard HEAD~1 :-
Hard Reset.

24. git revert HEAD :-
Creates a new commit instead of deleting previous commit but it deletes the code of previous commit.
You can see that previous commit in "git log"

# What Are -m, -M, -u This :-
These are called: flags / options, They modify command behavior.

1. -m :- means : message
git commit -m "message"

2. -u :- Meaning : Connect local branch with remote branch.
git push -u origin main

3. -M :- means : move/rename branch forcefully
git branch -M main ==> like Here: master → main

4. -v :- means : Shows verbose/view details.
git remote -v

5. -f :- means : Force push, Dangerous sometimes.
git push -f

6. -b :- means : Creates New Branch
git checkout -b feature-login

7. -d : use : for local branch deletion(IF MERGED)
git branch -d feature-login

8. -D : use : for local branch deletion(IF NOT MERGED MEANS FORCE DELETE)
git branch -D feature-login

# IF YOU EVER STUCK IN VIM EDITOR THEN SIMPLY DO THIS :
How To Exit :-
Simply press :-  q / ! / :qw / :q!

# There are TWO types of branch deletion :-
1. Delete Local Branch :-
git branch -d feature-login

IMPORTANT : -d works only if : ✅ branch already merged
Otherwise Git protects you.

If branch not merged:
Force Delete (Dangerous) :-
git branch -D feature-login
Capital -D = force delete.

2. Delete Remote Branch (GitHub) :-
To remove branch from GitHub:

git push origin --delete feature-login

<<<<<<< HEAD
This deletes : feature-login, from GitHub remote repo.

# GitHub Pull Request Workflow (PR Workflow) :-

A Pull Request means :
“Please review my branch and merge it into another branch.”
Usually : feature branch → main branch

# MOST IMPORTANT UNDERSTANDING :

PR is basically : a controlled merge request
with : review, discussion, testing, approval
before main gets modified.

# REAL INDUSTRY FLOW :-
git checkout -b feature
↓
write code
↓
git add .
git commit
↓
git push origin feature
↓
Open PR
↓
Review
↓
Merge PR
↓
git checkout main
git pull

# THIS is the standard workflow :-
Create feature branch
↓
Write code there
↓
Push feature branch
↓
Open Pull Request
↓
Team lead reviews
↓
Checks/tests run
↓
Conflicts resolved if any
↓
PR merged into main

# Real Visualization :-
main
│
├── feature-auth
├── feature-payment
├── feature-dashboard

Each developer works separately, Then PR merges features one-by-one safely.
Because : main branch should be protected.

# GIT RESET :- 
What is git reset?

git reset means: “Move Git back to an older state.”

Depending on type, it can : unstage files, remove commits, remove changes completely

# THREE TYPES OF RESET :- 

Reset Type	             What Happens
--soft	              remove commit only
--mixed	              remove commit + unstage
--hard	              remove EVERYTHING

SOFT RESET 
1. git reset --soft HEAD~1 :-
Meaning : remove last commit BUT keep all code and changes staged.  
USE CASE :-
Suppose : wrong commit message, then you can change through this.

MIXED RESET
2. git reset HEAD~1 :-
It means : git reset --mixed HEAD~1
Meaning : remove last commit and changes unstaged BUT keep all code.
USE CASE :-
Suppose : commit was wrong but code still needed. 

HARD RESET
3. git reset --hard HEAD~1 :- 
Meaning : remove last commit and remove all code, changes unstaged.
Hard reset can permanently destroy work.

First Understand HEAD :-
Git keeps pointer called : HEAD
Meaning : current/latest commit position

Syntax	      Meaning
HEAD~1	    one commit back
HEAD~2	    two commits back

# VERY COMMON USE CASES :-

Case 1 — Wrong Commit Message :-
git reset --soft HEAD~1
git commit -m "Correct message"

Case 2 — Accidentally Added Wrong Files :-
git reset
unstages files.

Case 3 — Completely Remove Last Commit :-
git reset --hard HEAD~1

# GIT REVERT :-

What is git revert?

git revert means :-
“Undo a commit safely by creating a new opposite commit.”

Command : git revert HEAD
Observe:
new commit created
old commit still visible
code reversed

IMPORTANT :-
❌ it does NOT delete history  
✅ it adds new history and it deletes previous commit or a specific commit through commit_ID which you can see it in "git log"
Use:
git revert COMMIT_ID

Example:
git revert a1b2c3

# GIT REBASE :-

Rebase says:
“Take my feature commits and replay them on top of latest main.”

# both merge and rebase finally combine code :
BUT The MAIN difference is : Git history (commits)

# Real Commit History Example :
If you run:
git log --oneline

You may see in source control:
M Merge branch 'main'
D Added JWT auth

Notice EXTRA that commit :
Merge branch 'main'
That commit exists ONLY because merge happened.

# git rebase main :
Using the above command NO extra merge commit created which created in git merge feature.
That’s why : rebase gives cleaner history

# REAL COMMIT LOG DIFFERENCE :
1. After Merge :
Merge branch 'main'
Added JWT auth
Added login page
Fixed dashboard bug
Added navbar
It looks Messier.

2. After Rebase :
Added JWT auth
Added login page
Fixed dashboard bug
Added navbar
It gives Cleaner linear flow.

# Merge Says :
"Let's combine histories"
So Git creates:extra merge commit

# Rebase Says :
"Let's replay my commits on latest main"
So:❌ no merge commit needed  

# THE BIGGEST DIFFERENCE :
Command	        Main Purpose
git pull	    Get latest remote code
git merge	    Combine two branches
git rebase	  Replay commits on updated base

# FINAL PUSH CONFUSION CLEARED :
Command	                Push Needed After?
git pull	        Usually NO (depends on scenrio)
git merge	        YES (coz after merge it updates only local, we have to update main also)
git rebase	      YES (same as merge)


# 📚 Git & GitHub Command Cheat Sheet :

#Module 1 — Initialize Repository :

Command	                            Description	                                                  When to Use
git init	       Initialize a new Git repository by creating the .git folder.	              First time adding Git to a p
git status	     Shows current branch, staged files, unstaged files, and untracked files.	  Before/after almost every Git operation.

#Module 2 — Staging Area :

Command	                      Description	                                          When to Use
git add .	        Stage all new and modified files.	                              Before committing.
git add <file>	  Stage only a specific file. 	                              When committing selected files only.
git reset	        Unstage all staged files (default = --mixed).	              Accidentally staged the wrong files.

#Module 3 — Commit :

Command	                                    Description	                                         When to Use
git commit -m "message"	            Save staged changes into Git history.	                     Every logical change.
git log	                            Show complete commit history.	                             View detailed history.
git log --oneline	                  Compact commit history.	                                       Daily usage.
git log --oneline --graph --all	    Visual branch graph.	                                     Understand branch history.

#Module 4 — Remote Repository :

Command	                                  Description	                                          When to Use
git remote add origin <url>	      Connect local repository to GitHub.	                           First push.
git remote -v	                    Show connected remote repositories.	                         Verify remote URL.

#Module 5 — Push & Pull :

Command	                                Description	                                              When to Use
git push -u origin main	          Push first time and set upstream.	                             Initial push.
git push	                        Push commits to remote.	                                    Daily use after first push.
git pull	                        Fetch + Merge remote changes.	                              Sync local branch with remote.
git fetch	                        Download remote changes without merging.	                  Check updates before merging.

#Module 6 — Branching :

Command	                                 Description	                                          When to Use
git branch	                        List local branches.	                                     View branches.
git branch <name>	                    Create branch.	                                        New feature/bugfix.
git checkout <branch>	                Switch branch.	                                        Move between branches.
git checkout -b <branch>	          Create and switch branch.	                                Most common workflow.
git branch -d <branch>	            Delete local branch.	                                    After merging feature.

#Module 7 — Remote Branches :

Command	                                     Description	                                        When to Use
git push origin <branch>	              Push a branch to GitHub.	                          First time pushing feature branch.
git push origin --delete <branch>	      Delete remote branch.	                                    After PR merged.

#Module 8 — Merge :

Command	                              Description	                                              When to Use
git merge <branch>	          Merge another branch into current branch.	                    Combine feature into main.

#Module 9 — Pull Request Workflow :

Step	        Action
1	      Create feature branch
2	        Write code
3	        git add .
4	      git commit -m "..."
5	      git push origin feature/...
6	      Open Pull Request
7	        Review
8	        Merge PR
9	      git pull on main
10	    Delete feature branch

#Module 10 — Merge Conflicts :

Command	                      Description
git add .	                 Stage resolved files.
git commit	          Finish merge after conflict resolution.

#Module 11 — Git Stash :

Command	                      Description	                                    When to Use
git stash	            Save unfinished work temporarily.	            Switch tasks without committing.
git stash list	      Show all stashes.	                                  View saved work.
git stash pop	        Restore and remove latest stash.	                  Continue work.
git stash apply	      Restore without deleting stash.	                  Reuse stash later.
git stash drop	      Delete one stash.	                                    Cleanup.
git stash clear	      Delete all stashes.	                          Remove all temporary work.

#Module 12 — Reset :

Command	                              Description	                                Effect
git reset --soft HEAD~1	      Remove last commit, keep changes staged.	      Commit removed.
git reset --mixed HEAD~1	    Remove commit and unstage files.	              Default reset.
git reset --hard HEAD~1	      Remove commit and delete changes.	                 Dangerous.

#Module 13 — Revert :

Command	                              Description	                                          When to Use
git revert HEAD	              Undo last commit by creating a new commit.	              Safe for shared history.
git revert <commit-id>	      Undo a specific commit.	                                  Production bug fixes.
git revert HEAD --no-edit	    Revert using default commit message.	                        Avoid editor.

#Module 14 — Rebase :

Command	                                      Description	                                                When to Use
git rebase main	                    Replay current branch commits on top of local main.	             Update feature branch.
git pull --rebase origin main	      Fetch latest remote main and rebase current branch onto it.	     Common company workflow.
git rebase --continue	              Continue after resolving conflict.	                                During rebase.
git rebase --abort	                        Cancel rebase.	                                         If something goes wrong.

#Module 15 — Push After Rebase :

Command	                                Description	                                        When to Use
git push --force-with-lease	    Safely replace remote history after rebase.	          After rebasing a pushed branch.
git push --force	              Force overwrite remote history.	                      Avoid unless absolutely necessary.

#Module 16 — Cherry Pick :

Command	                                    Description	                                    When to Use
git cherry-pick <commit-id>	      Copy one commit to current branch.	                 Hotfixes, production fixes.
git cherry-pick --continue	      Continue after conflict.	                           During cherry-pick.
git cherry-pick --abort	          Cancel cherry-pick.	                                 If conflicts become difficult.

#Module 17 — Useful Commands :

Command	                                        Description
git rev-list --count HEAD	                  Count total commits.
git status	                              Check repository state.
git branch	                         See current branch (* indicates active branch).